language: python
python:
  - "3.6"
# command to install dependencies
install:
  - pip install -r requirements.txt
# command to run tests
script:
  - echo "Skip"
after_success:
  - docker login -u $DOCKER_USERNAME -p $DOCKER_PASSWORD
  - sh build_docker_push.sh
